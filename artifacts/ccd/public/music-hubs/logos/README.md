# Music Hub logos

Verified partner logos used by the Music Hubs directory seed.

| File | Used by |
|------|---------|
| `essex-music-service.svg` | Greater Essex, Essex Music Service, Essex districts |
| `tri-borough-music-hub.png` | Tri-Borough Music Hub and its borough pages |

Copies of assets already under `/partners/`. Prefer these paths in `musicHubsDirectory.seed.json` so hub pages do not depend on partner-card layout.

Hubs without a file here stay **text-only** (no fake “Logo placeholder” chrome).
